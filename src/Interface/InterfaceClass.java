package Interface;

public interface InterfaceClass {

    //An interface in Java looks similar to Class. An Interface can have methods and variables jus like the class
    //But, methods declared in interface are by default abstract
    //Interface extends another Interface but interface cannot implement another Interface

    //The below method is y default treated as public abstract void myMethod
    //Below method has no body

    public void myMethod();

}
